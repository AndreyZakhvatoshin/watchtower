<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- Отладочный режим выключен принудительно: при APP_DEBUG=true тело
             ответа об ошибке несёт file, line и trace, а они различаются для
             двух разных точек отказа. Проверка «ответы неразличимы» на таком
             теле проверяла бы не то, что заявлено. -->
        <env name="APP_DEBUG" value="false" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Тесты идут против PostgreSQL, а не sqlite: timestamptz, типы колонок
             и поведение уникальных индексов на sqlite не проверяются в принципе,
             а на ступени 1 добавятся оконные функции и generate_series (AD-9).
             Хост, пользователь и пароль берутся из .env — в репозитории их нет. -->
        <!-- force="true" обязателен: без него <env> уступает переменной,
             уже экспортированной в окружении. Строка DB_DATABASE без force
             означает «watchtower_test, если снаружи ничего не задано» —
             а RefreshDatabase мигрирует ту базу, которую в итоге получил. -->
        <env name="DB_CONNECTION" value="pgsql" force="true"/>
        <env name="DB_DATABASE" value="watchtower_test" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
